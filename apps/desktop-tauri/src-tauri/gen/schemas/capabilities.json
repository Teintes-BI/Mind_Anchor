{"default":{"identifier":"default","description":"Phase 0 capabilities. Deliberately minimal: the app has no filesystem and no notification permission, so none are declared here. Network access goes through Rust/WinHTTP rather than the webview, so no HTTP permission is needed either. `core:window:allow-hide` and `allow-show` exist because closing the window puts the app in the tray instead of quitting.","local":true,"windows":["main"],"permissions":["core:default","core:window:allow-hide","core:window:allow-show","core:window:allow-unminimize","core:window:allow-set-focus"]}}