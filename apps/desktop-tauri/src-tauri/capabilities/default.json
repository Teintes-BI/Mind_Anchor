{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Phase 0 capabilities. Deliberately minimal: the app has no network, no filesystem and no notification permission, so none are declared here.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
